# json2csv

Convert json to csv.

## Build

Run the following command.

```bash
make build
```

Then execution file build in `dist/` directory.

## Development setup

Install dependencies.

```bash
make dev-setup
```
